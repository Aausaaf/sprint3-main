import React, { useContext } from "react";
import {NavLink} from 'react-router-dom'
const Navbar = () => {
  
    
  return <div style={{display:"flex"}} className="navbar">

  </div>;
};

export default Navbar;
